# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
node_modules
dist
dist-demo
dist-ssr
*.local

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# App-generated backups (the user's exported financial data — must never
# be committed)
spending-viz-backup-*.json
strictly-spending-backup-*.json
*.backup
*.backup.json

# Personal seed extras — your private merchant rules (see
# src/seed.local.example.ts). Never enters the public repo.
src/seed.local.ts

# Test fixtures (real bank CSVs live outside the repo; see parsers.test.ts)
fixtures/
test-fixtures/

# Local script output
screenshots/
mobile-screenshots/
*.webm
!docs/**/*.webm

# Coverage / cache
coverage/
.cache/
.turbo/

# Misc OS files
Thumbs.db
ehthumbs.db
